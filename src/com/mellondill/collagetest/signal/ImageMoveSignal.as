package com.mellondill.collagetest.signal
{
	import org.osflash.signals.Signal;
	
	public class ImageMoveSignal extends Signal
	{
		public function ImageMoveSignal(...parameters)
		{
			super(parameters);
		}
	}
}